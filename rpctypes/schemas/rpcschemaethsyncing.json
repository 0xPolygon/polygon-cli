{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "properties": {
        "startingBlock": {
            "type": "string",
            "pattern": "^0x[0-9a-f]{1,}$"
        },
        "currentBlock": {
            "type": "string",
            "pattern": "^0x[0-9a-f]{1,}$"
        },
        "highestBlock": {
            "type": "string",
            "pattern": "^0x[0-9a-f]{1,}$"
        }
    },
    "required": [
        "startingBlock",
        "currentBlock",
        "highestBlock"
    ]
}
